"""Analytics demo: a production-grade analytics agent over user-provided data sources.

Supports CSV files, SQL databases, Neo4j graphs, and Parquet — all profiled,
semantically modeled, and queried through governed read-only agent tools.
"""
